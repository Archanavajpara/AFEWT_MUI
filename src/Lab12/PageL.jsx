import * as React from 'react';
import PropTypes from 'prop-types';
import Tabs from '@mui/material/Tabs';
import Tab from '@mui/material/Tab';
import Box from '@mui/material/Box';
import { Typography } from '@mui/material';


function CustomTabPanel(props) {
  const { children, value, index, ...other } = props;

  return (
    <div
      role="tabpanel"
      hidden={value !== index}
      id={`simple-tabpanel-${index}`}
      aria-labelledby={`simple-tab-${index}`}
      {...other}
    >
      {value === index && <Box sx={{ p: 3 }}>{children}</Box>}
    </div>
  );
}

CustomTabPanel.propTypes = {
  children: PropTypes.node,
  index: PropTypes.number.isRequired,
  value: PropTypes.number.isRequired,
};

function a11yProps(index) {
  return {
    id: `simple-tab-${index}`,
    'aria-controls': `simple-tabpanel-${index}`,
  };
}

export default function PageL() {
  const [value, setValue] = React.useState(0);

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
    <>
    <div>
    <Box sx={{ width: '100%', bgcolor: 'lightgrey', marginTop: 0, height: '60px' , textAlign: 'center', paddingTop: '10px'}}>
      <Box sx={{ borderBottom: 1, borderColor: 'divider' }}>
        <Tabs value={value} onChange={handleChange} aria-label="basic tabs example">
          <Tab label="Item One" {...a11yProps(0)} />
          <Tab label="Item Two" {...a11yProps(1)} />
          <Tab label="Item Three" {...a11yProps(2)} />
        </Tabs>
      </Box>
      <CustomTabPanel value={value} index={0} >
        Item One
      </CustomTabPanel>
      <CustomTabPanel value={value} index={1}>
        Item Two
      </CustomTabPanel>
      <CustomTabPanel value={value} index={2}>
        Item Three
      </CustomTabPanel>
      <Typography sx={{bgcolor:'cyan',width:'100%'}}>Lorem ipsum dolor sit amet consectetur adipisicing elit. 
        Dicta eligendi omnis exercitationem maxime, velit animi necessitatibus temporibus,
         iste ullam aspernatur illum asperiores sed excepturi id quam alias fugit repellat distinctio.</Typography>
    </Box>

    <Box sx={{ width:'screen', marginTop: 85, textAlign: 'center', bgcolor: '#1976d2', color: 'white',height: '20px'}}>
    © 2024 Your Company Name. All rights reserved.
    </Box>

    </div>
    </>
    );
}

